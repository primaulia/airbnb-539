<p id="notice"><%= notice %></p>

<p>
  <strong>Address:</strong>
  <%= @flat.address %>
</p>

<p>
  <strong>Flat Type:</strong>
  <%= @flat.flat_type %>
</p>

<% if @flat.banner.attached? %>
  <%= cl_image_tag @flat.banner.key, width: 150, height: 150 %>
  <img src="" alt="">
<% end %>

<%= link_to 'Edit', edit_flat_path(@flat) %> |
<%= link_to 'Back', flats_path %>
